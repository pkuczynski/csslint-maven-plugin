<project>
   <modelVersion>4.0.0</modelVersion>
   <groupId>csslint-maven-plugin.it</groupId>
   <artifactId>test-custom-rules</artifactId>
   <version>1.0</version>

   <build>
      <plugins>
         <plugin>
            <groupId>net.csslint</groupId>
            <artifactId>csslint-maven-plugin</artifactId>
            <version>@project.version@</version>
             <configuration>
                 <rules>
                     <rule>ids</rule>
                     <rule>important</rule>
                     <rule>import</rule>
                 </rules>
                 <includes>
                     <include>file1.css</include>
                 </includes>
                 <format>lint-xml</format>
             </configuration>
            <executions>
               <execution>
                   <phase>package</phase>
                  <goals>
                     <goal>check</goal>
                  </goals>
               </execution>
            </executions>
         </plugin>
      </plugins>
   </build>
</project>